I'll help create the `SlugGenerator` service and its tests. This is a multi-step task.

I'll start by creating a to-do list to track the implementation.

*   Create `src/Service/SlugGenerator.php` with the service class structure.
*   Implement the `generateSlug` method, using dependency injection for `SluggerInterface`.
*   Create `tests/Service/SlugGeneratorTest.php` with PHPUnit tests.
*   Write at least four test cases: basic text, text with accents, special characters, and multiple spaces.
*   Run necessary tests to verify the implementation.